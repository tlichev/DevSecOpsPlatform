! Interface Configuration - generated by DevSecOps Platform
{% for intf in interfaces %}
interface {{ intf.name }}
 description {{ intf.description | default("") }}
{% if intf.mode == "access" %}
 switchport mode access
 switchport access vlan {{ intf.vlan }}
{% elif intf.mode == "trunk" %}
 switchport mode trunk
 switchport trunk allowed vlan {{ intf.trunk_vlans | join(",") }}
{% elif intf.mode == "routed" %}
 no switchport
 ip address {{ intf.ip_address }} {{ intf.netmask }}
{% endif %}
{% if intf.shutdown | default(false) %}
 shutdown
{% else %}
 no shutdown
{% endif %}
 spanning-tree portfast{% if intf.mode == "trunk" %} trunk{% endif %}

!
{% endfor %}
